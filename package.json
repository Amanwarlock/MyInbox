{
  "name": "my-inbox",
  "version": "1.0.0",
  "description": "MEAN stack app with socket.io for real time rendering",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AmanKareem",
  "license": "ISC",
  "dependencies": {
    "gulp": "^3.9.1"
  }
}
